# Example encrypted .env file managed by SOPS.
# Run `sops --encrypt --age <recipient> .env` to produce a file like this.
# This placeholder demonstrates the expected YAML structure for SOPS metadata.

sops:
    kms: []
    gcp_kms: []
    azure_kv: []
    hc_vault: []
    age:
        - recipient: age1examplexxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
    encrypted_regex: '^(POSTGRES|AUTHELIA|CROWDSEC|CLOUDFLARE)'
    version: '3.8.1'
